In order to compile shadow3 in windows, using the basic gfortran
compiler (which does not include preprocessor) I first run the 
preprocessor in linux for all the files. This is done in this directory.

./go.sh

